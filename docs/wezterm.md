# WezTermの備忘録（時間を溶かさないため）

## Space を切り替えずに WezTerm を使う

複数のデスクトップ（Space）を利用している場合、WezTerm をアクティブにすると WezTerm が存在する Space に移動してしまう。

以下の設定を行うことで、現在の Space を維持したまま WezTerm を利用できる。

1. Dock の WezTerm アイコンを右クリック
2. `Options` → `Assign To` → `All Desktops`

> WezTerm の設定ではなく macOS の設定。
